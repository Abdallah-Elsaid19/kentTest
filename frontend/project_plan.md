# Frontend implementation

The original mock-only prototype plan has been superseded. The frontend now consumes the versioned Django API through TanStack Query; React content, navigation, site settings, metadata and forms are not sourced from local mocks.

See the root `README.md` and `docs/ARCHITECTURE.md` for the active implementation and migration plan.
